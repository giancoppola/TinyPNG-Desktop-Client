import { App } from '../../types';
import { SetStateAction, useEffect, useState, Dispatch } from 'react';

import { Displays } from './_displays';

export const SysInfo = () => {
    return (
        <Displays/>
    )
}